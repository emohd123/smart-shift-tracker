
import { CardHeader, CardTitle } from "@/components/ui/card";

export default function ShiftFormHeader() {
  return (
    <CardHeader>
      <CardTitle>Create New Shift</CardTitle>
    </CardHeader>
  );
}
